@page
@model RegisterModel
@{
    ViewData["Title"] = "Register";
    ViewData["Header"] = "Create an account to view and record your service hours.";
}

<div class="row">
    
    <div class="col-md-4">
        <form asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div class="form-group">
                <label asp-for="Input.Name"></label>
                <input asp-for="Input.Name" class="form-control"/>
                <span asp-validation-for="Input.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control"/>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
                @{
                    if(Model.ANGERY != "no")
                    {
                        <span class="text-danger">@Model.ANGERY</span>
                    }
                }
            </div>
            <div class="form-group">
                <label asp-for="Input.Grade" class="control-label"></label>
                @Html.DropDownList("Input.Grade", Html.GetEnumSelectList<Grade>(), "", new {@class = "form-control"})
                <span asp-validation-for="Input.Grade" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Input.Password"></label>
                <input asp-for="Input.Password" class="form-control"/>
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Input.ConfirmPassword"></label>
                <input asp-for="Input.ConfirmPassword" class="form-control"/>
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-default">Register</button>
        </form>
    </div>

    <div class="col-md-4">
        <p>Upon registering, you will be sent a verification email to verify your account. An unverified account will be deleted one month after its creation. If you do not receive an email, you can send another one from the "Manage Account" page when you are logged in.</p>
        <br/>
        <p>Also, please make sure to use the same name for your account that you will use on your membership form. This way, we can verify that you paid your dues. Accounts with unpaid dues will be deleted at the end of the first quarter. Please contact us if this is an issue, we can easily make exceptions.</p>
        <br/>
        <p>Lastly, accounts belonging to seniors will be deleted at the start of the school year following graduation. If you do not want your account to be deleted, please let us know.</p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
